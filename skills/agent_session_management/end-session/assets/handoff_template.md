# Project Handoff

<!--
  Managed by the end-session / init-session skills.
  Section order is fixed. Do not add, remove, or reorder `##` headings —
  the parser splits on them and will drop anything it does not recognise.

  Size discipline: this file is read at the start of every session, so it is
  compacted on every write rather than appended to. Full session detail lives
  in .agent_docs/archive/ instead.
-->

## Project Snapshot

<!-- Stable facts only: what this project is, stack, architecture, entry points,
     how to run/test. Rewrite only when these actually change. -->

## Cumulative Learnings

<!-- Durable, non-obvious facts worth carrying forever: gotchas, constraints,
     conventions, dead ends already explored. Merged and deduped on write.
     If a line would be obvious from reading the code, it does not belong here. -->

## Last Session

<!-- 3-5 compressed bullets. Overwritten each session — never stacked. -->

## Current Session

<!-- Full detail for the session that just ended. Archived, then replaced,
     on the next write. -->
